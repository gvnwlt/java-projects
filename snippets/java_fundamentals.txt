# structure (write once, run everywhere)

JDK
  -syntax
  -data types
  -control flow
  -object-oriented

JRE (SE, EE, ME, FX)
  -configuration
  -security
  -threading
  -input/output

IDE for Java 
    Netbeans only supports newere Java with the Apache version 

    IntelliJ supports Java SE and Android right out of the box*

What 
  -language (complies into application byte code for the jvm--> turns the byte code into actual machine instructions)
  -runtime environment
  -standard library  